import React from 'react';
import { Link } from 'react-router-dom';
import { Button, Navbar } from 'flowbite-react';

const Home: React.FC = () => {
  return (
    <Button color="primary">Flowbite Button</Button>
  );
};

export default Home;
